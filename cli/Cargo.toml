[package]
name = "abrasive-cli"
version = "0.0.0"
edition = "2024"

[dependencies]
clap = { version = "4", features = ["derive", "color", "help"] }
thiserror = "2.0.18"
abrasive-protocol = { path = "../protocol" }